# iOS：cdylib 链接时 tauri 的 Swift 层符号由 Xcode 工程的 Pod 提供，
# 单独 cargo 链接 cdylib 时放行未定义符号（staticlib/rlib 不受影响）
[target.aarch64-apple-ios]
rustflags = ["-C", "link-arg=-Wl,-undefined,dynamic_lookup"]

[target.aarch64-apple-ios-sim]
rustflags = ["-C", "link-arg=-Wl,-undefined,dynamic_lookup"]
